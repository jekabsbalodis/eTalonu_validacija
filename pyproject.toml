[project]
name = "etalonu-validacija"
version = "0.1.0"
description = "Display statistics of Riga city public transportation ticket validations"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.4.0",
    "streamlit>=1.49.1",
]

[dependency-groups]
dev = [
    "pylint>=3.3.8",
]

[tool.ruff.format]
quote-style = "single"
